= stylesheet_link_tag 'nocontent'

.nosites
  - if @editors.empty?
    .inside
      Currently there are no editors.
      %br
      Invite others so they can update content as well.
      %br
  
      = render "layouts/action_bars/add_editor"

  - else
    .inside
      Select an editor in the sidebar.
  .helper