- sort_users(@owners).each do |owner|
  - selected = (owner == @owner)
  %li{:id => dom_id(owner), :class => selected ? "selected" : ""}
    - link_to admin_audit_path(owner), :class => 'label', :title => owner.user_name do
      %span.text= owner.user_name
    .menu-actions
      = render "admin/owners/hold_owner", :owner => owner
