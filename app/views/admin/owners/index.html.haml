%h2 Owners

%table.data
  %tr
    %th Name
    %th Email
    %th Plan
    %th Created
    %th Enabled
  - @owners.each do |owner|
    %tr
      %td= link_to(owner.name, admin_owner_path(owner))
      %td=h owner.email
      %td=h owner.plan
      %td=h owner.created_at
      %td=h owner.enabled?
