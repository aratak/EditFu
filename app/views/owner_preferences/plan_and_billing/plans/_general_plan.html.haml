= form.radio_button 'plan_id', plan.id, :class => "radio #{plan.payment_style_class} #{plan.current_style_class(@owner)}"
.plan-label
  %label{ :for => plan.label_tag_name }
  %dl
    %dt= "#{plan.display_name} #{plan.str_price}"
    %dd= "#{plan.description}"
  - if @owner.plan_was == plan
    %small Current