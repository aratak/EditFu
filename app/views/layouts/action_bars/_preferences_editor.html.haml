- if current_user.owner?
  = link_to_remote '<span>Editor Preferences</span>', :url => edit_editor_path(editor), :method => :get, :html => { :title => 'Editor Preferences', :class => "action-bar-button preferences-action" }