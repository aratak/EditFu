- if current_user.owner?
  = link_to_function "", "showConfirmPopup('#{site_path(site)}', 'Are you sure you want to remove the site \"#{site.name}\"?')", |
      :class => 'action-bar-button delete-action', |
      :title => "Remove this site" |
