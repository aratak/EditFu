%li{:id => id, :class => css_class}
  - link_to href, :class => 'label', :title => text do
    - if selected
      %img(src="/images/#{name}-icon.png")
    - else
      %img(src="/images/blank.png" width='14' height='13')
    %span.text= text

