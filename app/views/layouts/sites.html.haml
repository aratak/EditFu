- inside_layout 'application' do
  = content_for :sourceBar do
    %ul#top-menu
      - current_user.sites.each do |site|
        = render 'layouts/site_menu_item', :site => site

  - @content_for_tab = "pages"
  - @content_for_new_href = new_site_path
