- inside_layout 'member' do
  = content_for :sourceBar do
    = render 'layouts/site_top_menu'

  = content_for :tabs do
    = render "layouts/tabs", :tab => "pages"    

  = render "layouts/add_action_bar", :url => new_site_path
  
  = yield
