page.insert_html :bottom, "site-#{@page.site.id}-menu-ul", 
  :partial => 'layouts/page_menu_item', 
  :locals => { 
    :page => @page, :selected => nil
  }
page.insert_html :bottom, 'pages_form', :partial => 'sites/page', :locals => { :page => @page }
hide_popup(page, 'page.created')
